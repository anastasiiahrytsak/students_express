<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>Управління автомобілями</title>
    <style>
        body { font-family: Arial, sans-serif; padding: 20px; max-width: 800px; margin: auto; background-color: #1a1d23; color: #f5f5f5; }
        h2 { color: #f5f5f5; }
        .form-group { margin-bottom: 15px; }
        .form-group label { display: block; margin-bottom: 5px; font-weight: bold; color: #a0acb9; }
        
        .form-group input { 
            width: 100%; padding: 10px; box-sizing: border-box; 
            background-color: #1a1d23; border: 1px solid #4a5568; 
            color: #f5f5f5; border-radius: 4px; 
        }
        .form-group input::placeholder { color: #64748b; }
        .card { background: #252932; padding: 20px; border-radius: 8px; margin-bottom: 40px; border: 1px solid #2d3748; }
        .car-item { background: #252932; border: 1px solid #2d3748; padding: 15px; margin-bottom: 15px; border-radius: 5px; }
        
        .btn { padding: 10px 15px; cursor: pointer; border: none; border-radius: 4px; color: white; margin-right: 5px; text-decoration: none; display: inline-block; font-size: 14px;}
        .btn-submit { background-color: #2ecc71; font-size: 16px; width: 100%; margin-top: 10px; font-weight: bold; color: #1a1d23;}
        .btn-submit:hover { background-color: #27ae60; }
        
        .btn-edit { background-color: #f59e0b; color: #1a1d23; font-weight: bold;}
        .btn-delete { background-color: #ef4444; font-weight: bold;}
        .btn-back { background-color: #4a5568; margin-bottom: 20px; }
        .btn-back:hover { background-color: #2d3748; }
    </style>
</head>
<body>
    <a href="/" class="btn btn-back">⬅ Go back</a>

    {{#if editingCar}}
        <a href="/cars" class="btn btn-back">⬅ Скасувати редагування</a>
        <h2>Редагувати автомобіль: {{editingCar.car_brand}} {{editingCar.car_model}}</h2>
    {{else}}
        <h2>Додати новий автомобіль</h2>
    {{/if}}
    
    <div class="card">
        <form action="{{#if editingCar}}/cars/edit/{{editingCar.id}}{{else}}/cars{{/if}}" method="POST">
            
            <div class="form-group">
                <label for="car_brand">Марка авто (Brand):</label>
                <input type="text" id="car_brand" name="car_brand" value="{{editingCar.car_brand}}" placeholder="Наприклад: Audi" required>
            </div>

            <div class="form-group">
                <label for="car_model">Модель (Model):</label>
                <input type="text" id="car_model" name="car_model" value="{{editingCar.car_model}}" placeholder="Наприклад: A6" required>
            </div>

            <div class="form-group">
                <label for="engine_type">Тип двигуна (Engine Type):</label>
                <input type="text" id="engine_type" name="engine_type" value="{{editingCar.engine_type}}" placeholder="Наприклад: Бензин 3.0" required>
            </div>

            <div class="form-group">
                <label for="horsepower">Кінські сили (Horsepower):</label>
                <input type="number" id="horsepower" name="horsepower" value="{{editingCar.horsepower}}" placeholder="Наприклад: 340" required>
            </div>

            <div class="form-group">
                <label for="weight">Вага, кг (Weight):</label>
                <input type="number" id="weight" name="weight" value="{{editingCar.weight}}" placeholder="Наприклад: 1800" required>
            </div>

            <div class="form-group">
                <label for="acceleration_0_to_100">Розгін 0-100 км/год (с):</label>
                <input type="number" step="0.1" id="acceleration_0_to_100" name="acceleration_0_to_100" value="{{editingCar.acceleration_0_to_100}}" placeholder="Наприклад: 5.1" required>
            </div>

            <div class="form-group">
                <label for="price">Ціна, $ (Price):</label>
                <input type="number" id="price" name="price" value="{{editingCar.price}}" placeholder="Наприклад: 55000" required>
            </div>

            <button type="submit" class="btn btn-submit">
                {{#if editingCar}}Зберегти зміни{{else}}Створити запис{{/if}}
            </button>
        </form>
    </div>

    <hr style="border-color: #2d3748;">

    <h2>Список автомобілів в базі</h2>
    
    <div id="cars-list">
        {{#each cars}}
            <div class="car-item" id="car-{{this.id}}">
                <h3 style="margin-top: 0; color: #2ecc71;">{{this.car_brand}} {{this.car_model}}</h3>
                <p>
                    <strong>Двигун:</strong> {{this.engine_type}} | 
                    <strong>Потужність:</strong> {{this.horsepower}} к.с. <br>
                    <strong>Вага:</strong> {{this.weight}} кг | 
                    <strong>Розгін 0-100:</strong> {{this.acceleration_0_to_100}} с <br>
                    <strong>Ціна:</strong> ${{this.price}}
                </p>
                <p><small style="color: #a0acb9;">Додано: {{this.created_at_date}} о {{this.created_at_time}}</small></p>
                
                <a href="/cars/edit/{{this.id}}" class="btn btn-edit">Редагувати</a>
                <button class="btn btn-delete delete-btn" data-id="{{this.id}}">Видалити</button>
            </div>
        {{else}}
            <p style="color: #a0acb9;">У базі поки що немає жодного автомобіля. Додайте перший!</p>
        {{/each}}
    </div>

    <script>
        document.querySelectorAll('.delete-btn').forEach(button => {
            button.addEventListener('click', async (event) => {
                const carId = event.target.getAttribute('data-id');
                if (confirm('Ви впевнені, що хочете назавжди видалити цю машину?')) {
                    try {
                        const response = await fetch(`/cars/${carId}`, { method: 'DELETE' });
                        if (response.ok) {
                            document.getElementById(`car-${carId}`).remove();
                        } else {
                            alert('Виникла помилка при видаленні.');
                        }
                    } catch (error) {
                        console.error('Помилка:', error);
                    }
                }
            });
        });
    </script>
</body>
</html>
